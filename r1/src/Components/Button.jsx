function Button({clickButton, clickPlus}) {
    return (
        <>
        <button onClick={clickButton}>Make pink</button>
        <button onClick={clickPlus}>nr</button>
        </>
    )
}

export default Button;